<!-- {/* </div><img src={`${base_url}${image.backdrop_path || image.poster_path}`} alt='' /> */}

			{/* <TextTruncate line={1} element='p' truncateText='...' text={image.overview} /> */}

			{/* <h2>{image.title || image.original_name}</h2> */}
			{/* <p>number of likes </p> */} -->

		